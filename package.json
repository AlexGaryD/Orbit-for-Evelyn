{
  "name": "orbit-for-evelyn",
  "version": "1.0.0",
  "description": "Un sitio especial para Evelyn Manzanita Navas",
  "private": true,
  "scripts": {
    "dev": "npx serve .",
    "build": "mkdir -p public && cp index.html public/ && cp -r assets recuerdos public/ && cp Cumple.mpeg public/",
    "start": "npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
